services:
  - type: web
    name: rimo-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build && mkdir -p uploads
    startCommand: node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
